name: probe-api
on:
  push:
    branches:
    - claude/lucid-fermat-8vtoo1
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Chay ham that voi API that
      run: node probe-that.js
